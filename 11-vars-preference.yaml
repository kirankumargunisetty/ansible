- name: variable preference
  hosts: frontend
  vars:
    full_name: "siva from play"
  vars_files:
  - course.yaml
  vars_prompt:
    - name: full_name
      prompt: "Please enter your full_name"
      private: false
  tasks:
  - name: print vars preference
    vars:
      #full_name: "siva from task level"
    debug:
      msg: "Hi,{{ full_name }}"



# 1. CMD args will be first preferecne
# 2. Task level

