- name: demo on loops
  hosts: localhost
  connection: local
  tasks:
  - name: print names
    debug:
      msg: "Hi {{ item }}" # item is reserved special variable
      loop:
      - kiran
      - kumar
      - ekshith