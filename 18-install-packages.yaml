- name: install packages
  hosts: frontend
  become: Yes
  tasks:
  - name: install packages
    packages:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - mysql
    - mongodb-mongosh
    - git